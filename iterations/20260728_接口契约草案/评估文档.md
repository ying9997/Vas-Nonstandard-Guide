# 评估文档 - 中间层接口契约草案

> 日期：2026-07-28  
> 模块：`module_非标页内嵌智能引导/`

## 1. 可行性评估

- 技术可行性：通过。前置归一化契约已定义候选输入结构、枚举和 `routeBranch`；HTML 原型已验证前端消费 `route`、`pageActions`、`fieldSuggestions`、`matchState`、`matchConfidence` 的形态。
- 资源可行性：通过。本次只写文档，不依赖接口联调、服务部署或测试环境。
- 边界可行性：通过。本次不改归一化契约、不改 HTML 原型、不写中间层实现、不写评测用例、不做费用预估和视频 SOP 接口。

## 2. 每节点成功标准与自检方法

| 节点编号 | 成功标准 | 自检方法 |
|----------|----------|----------|
| N1 | 已引用现有归一化契约和 HTML 原型字段，不凭空改写前置字段 | 检查文档字段是否承接 `systemScopedVascList`、`routeBranch`、`pageActions` |
| N2 | 请求 schema 覆盖前端、中间层、Agent 输入链路 | 检查 `接口契约草案.md` 是否含三段请求结构 |
| N3 | 响应 schema 覆盖 Agent、中间层、前端输出链路 | 检查 `接口契约草案.md` 是否含三段响应结构 |
| N4 | 决策、会话、兜底、阈值、`return` 扩展均有明确规则 | 搜索对应章节和枚举表 |
| N5 | 字段清单覆盖 request / response 全字段，标注必填、来源、消费方 | 检查 `字段清单_table.md` 的表头和字段完整性 |
| N6 | 只新增本 workflow 文档，无前置产物变更 | 使用 `git diff --name-only` 检查变更范围 |

## 3. 风险缓解措施

- 字段过度承诺风险：所有接口均标注为草案，不写实现算法和联调承诺。
- Agent 越权路由风险：在决策流中写明只能取用中间层注入的 `routeBranch`，禁止基于名称或语义自行改写。
- 低置信误填风险：正式写入 `matchConfidence` 阈值，低于阈值只允许兜底追问或人工处理。
- `serviceDomain=return` 误入 2b 风险：单独写明退货域默认不进入核心分支，预留扩展入口。

## 4. 评估结论

通过。本切片可执行，交付形态为文档新增，风险可由边界说明和字段清单控制。

