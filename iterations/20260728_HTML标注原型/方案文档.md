# 方案文档 - HTML 标注原型

> 日期：2026-07-28  
> 模块：`module_非标页内嵌智能引导/`  
> 本次目标：输出单一交付文档 `原型_接口注入字段与落点标注.md`。

## 1. 任务目标

基于本模块 `README.md`、`HANDOFF.md`、既有方案文档和线上非标页快照，输出第一阶段 HTML 标注原型文档。文档必须先补齐 Opus 复审 5 项问题，再给出页面区域、交互按钮、页面动作、延迟兜底和三类分支展示差异，最后明确本期边界。

## 2. 节点明细

| 节点 | 输入 | 操作 | 输出 | 依赖 |
|------|------|------|------|------|
| N1 基准读取 | README、HANDOFF、既有方案 | 对齐 2d/2a/2b 分支、结构化字段、本期不做 | 基准口径清单 | 本地文档 |
| N2 实物验证 | 页面快照、接口文档、SOP 模板 | 验证页面字段和接口字段，不猜测 | 页面落点与依赖口径 | HTML 快照、接口文档 |
| N3 复审补全 | Opus 5 问题 | 固化字段、枚举、阈值、状态边界 | 第一章节内容 | README/HANDOFF |
| N4 页面标注 | 线上非标页快照 | 标注 AI 指引框、按钮、附件提示、分支展示 | 第二章节内容 | 页面快照 |
| N5 边界约束 | README/HANDOFF | 同步本期不做和从属关系 | 第三章节内容 | HANDOFF |

## 3. 影响评估

本次只新增文档，不改代码、不改接口、不输出评测集、不修改前端实现包。对现有模块的影响是补齐 HTML 原型标注切片，供产品、前端、中间层对齐。

## 4. 回退方案

如后续发现字段口径冲突，回退方式是只修订新增的 `原型_接口注入字段与落点标注.md`，不影响既有方案、评估和运行时材料。
